# Staging E2E evidence (gitignored values)

Put dated folders here locally, e.g. `staging-2026-07-23/`.

Do **not** commit screenshots that contain raw tokens, full cookie values, or passwords.
Mask or crop secrets before sharing for approval.

